import 'package:flutter/material.dart';

class GlobalAddButton extends StatelessWidget {
  const GlobalAddButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
